apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netflixMovieCatalog.fullname" . }}-test-connection"
  labels:
    {{- include "netflixMovieCatalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netflixMovieCatalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
