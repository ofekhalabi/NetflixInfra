apiVersion: apps/v1
kind: Deployment
metadata:
  name: netflixmoviecatalog-dev
  labels:
    app: netflixmoviecatalog-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: netflixmoviecatalog-dev
  template:
    metadata:
      labels:
        app: netflixmoviecatalog-dev
    spec:
      containers:
        - name: server
          image: ofekhalabi/netflix-movie-catalog:23
          resources:
            requests:
              cpu: 100m
              memory: 300Mi
            limits:
              cpu: 100m
              memory: 300Mi
            livenessProbe:
              initialDelaySeconds: 10
              periodSeconds: 10
              failureThreshold: 3
              httpGet:
                path: "/live"
                port: 8080
            readinessProbe:
              initialDelaySeconds: 5
              periodSeconds: 10
              failureThreshold: 3
              httpGet:
                path: "/ready"
                port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: netflixmoviecatalog-service-dev
spec:
  selector:
    app: netflixmoviecatalog-dev
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
